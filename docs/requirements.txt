# Docs-only dependencies. Kept minimal so RTD builds quickly and stays
# under the free-tier memory limit; the heavy ML stack is mocked by
# `autodoc_mock_imports` in conf.py rather than installed.

sphinx>=7,<9
sphinx-rtd-theme>=2
myst-parser>=2                  # lets us include the project README.md
sphinx-autodoc-typehints>=1.25  # nicer type-hint rendering
sphinx-copybutton>=0.5          # adds a copy-to-clipboard button on code blocks
